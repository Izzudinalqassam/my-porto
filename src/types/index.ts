// src/types/index.ts
export interface TechStackCategory {
  id: string;
  title: string;
  icon: string;
  items: string[];
}

export interface Strength {
  id: string;
  title: string;
  icon: string;
  description: string;
}

export interface Achievement {
  id: string;
  icon: string;
  title: string;
  description: string;
}